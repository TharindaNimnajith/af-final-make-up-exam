const authStoreKey = '@af2021'

const apiBaseUrl = 'http://localhost:5000/'

const authApi = `${apiBaseUrl}auth/`
const uploadsApi = `${apiBaseUrl}uploads/`
const usersApi = `${apiBaseUrl}users/`
const roomsApi = `${apiBaseUrl}rooms/`

export {
  apiBaseUrl,
  authStoreKey,
  authApi,
  uploadsApi,
  usersApi,
  roomsApi
}
